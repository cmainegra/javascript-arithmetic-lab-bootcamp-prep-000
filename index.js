function add(a,b) {
  return a + b }
function subtract(a,b) {
  return a - b } 
function multiply(a, b) {
  return a * b }
function divide(a,b) {
  return a / b }
function inc(n) {
  var number = n++ 
  return n }
function dec(n) {
  var number = n-- 
  return n }
function parseInt(n) {
  return parseInt(n) }
function preserveDecimal(n) {
  return parseFloat(n) }
function makeInt(n) {
  return `toString(n)`
}
function makeInt(n) {
  return `toEqual(n)`
}